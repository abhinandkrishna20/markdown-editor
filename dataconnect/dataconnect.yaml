specVersion: "v1beta"
serviceId: "markdown-editor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "markdown-editor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
